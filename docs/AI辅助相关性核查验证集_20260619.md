# AI辅助相关性核查验证集（358条）

生成日期：2026-06-19

## 输入来源

- `manual_returned_298`：用户提供版本 `exam_298.jsonl`，答案由 `data/derived/manual_review_returned_20260619.csv` 的 `manual_relevance` 回填。
- `guideline_review_60`：早期 60 条 guideline review，来源 `data/review/review_final_labels_after_guideline_review.csv`，使用 `final_label` 作为标准答案。

## 产出

- `data/derived/ai_audit_validation_exam_358_20260619.jsonl`：给模型看的考试题，不含答案。
- `data/derived/ai_audit_validation_answer_key_358_20260619.json`：358 条标准答案。
- `_incoming_ai_audit_files/exam_358_with_early60.jsonl` / `_incoming_ai_audit_files/answer_key_358.json`：放在用户文件旁边的便捷副本。
- `scripts/score_ai_audit.py`：修正版判分脚本。

## 标签分布

- 相关：60
- 不相关：288
- 拿不准：10
- 总计：358

## 对用户原版文件的主要检查结论

1. `score.py` 硬编码读取 `/home/claude/ai_relevance/answer_key_298.json`，在当前项目不可复现；已改为 `--answer-key` 参数。
2. 原 298 条负样本占比高，总一致率容易虚高；补入 60 条后相关/不相关更均衡，并在新脚本中加入相关精确率、不相关精确率、误报率、拿不准比例。
3. 原脚本只接受完全一致的中文 verdict；新脚本会把 `relevant/irrelevant/uncertain`、`likely_relevant/low_relevance/needs_review` 归一到中文标签。
4. 原脚本当没有匹配记录时会除零；新脚本会明确退出。
5. 用户版 `exam_298.jsonl` 第 226 行是占位 id `row225`，对应人工回流表中 URL 异常的京剧相关样本；答案已按行序补为“相关”，但后续最好补回真实 TikTok video_id。
6. `judging_prompt.md` 标题写 YouTube，但验证集是 TikTok；判定规则本身可复用，建议改成“视频元数据相关性判断”，避免模型误以为平台特定。
