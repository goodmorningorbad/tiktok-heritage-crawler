# Claude Design 交接包 ·《中国非遗走出去了。但，被看到了吗？》

2026-06-27 打包。这个文件夹是自包含的——把它整个上传到 Claude Design 即可继续微调，
不需要回原仓库找东西。

## 这是什么

一篇关于"中国 44 项 UNESCO 非遗在 TikTok 海外能见度"的数据新闻。
当前进度：**叙事 + 数据已定稿；视觉走「传统 × 算法的碰撞 / 电光故障」方向，开篇样板已做出来，正在微调。**

## 先看哪个

1. **`samples/recognition_test.html`** —— 已定稿的**开篇样板**（识别实验）。双击打开能点：
   选卡 → 揭晓 → 撞词卡被红章拍上。这版定下了全站美学语言。**微调从这里继续。**
2. **`design_spec.md`** —— 为什么这么设计 + 色彩语义 + 招牌动作 + 叙事弧 + **待微调清单**。
3. **`design_tokens.css`** —— 可直接复用的色板/字体/组件 CSS 变量与类。

## 文件清单

```
README.md                  ← 你在这
方案_v4.md                  叙事方案全文（含"衡量口径"红线：测能见度非影响力，稀释是透镜非标尺）
design_spec.md             设计规格 + 待微调清单
design_tokens.css          色板/字体/招牌动作/组件 CSS
samples/
  recognition_test.html    ★ 开篇样板（已定稿美学，可点交互）
  story_full_v3.html        上一版完整故事页（深色但偏温吞，仅作"全部章节+数据长啥样"的参考；美学已被新样板取代）
assets/
  motif_papercut.svg        窗花主视觉（RGB 撕裂版）
  chart_scatter.svg / chart_dumbbell.svg / chart_author.svg   旧版分析图（参考，待按新美学重做成"一图一结论"）
data/
  figures_data.json         ★ 页面要显示的全部数字（44项触达/塌缩/命名/人工查实/跨平台），compact 可直接用
  recognition_rounds.json   ★ 识别实验卡片（真实视频文案，真伪+标签+撞词率）
```

## 用法建议（在 Claude Design 里）

- 以 `recognition_test.html` 为风格基准，按 `design_spec.md` 第七节的叙事弧把其余章节补齐。
- 所有数字从 `data/*.json` 取，别硬编码——尤其 `figures_data.json.projects`（44 项主数据）
  和 `recognition_rounds.json`（卡片）。
- 守住 `design_spec.md` 第二节的**衡量口径红线**（2026-06-27 终态）：**绝对真实播放=标尺（隐形判定/排序只看它），份额=透镜（份额会骗人：书法份额仅12%但绝对8500万=被大量看见）**；44 项按 `fate` 四象限排（独占且被看见 / 挤着但被看见 / 独占但小众 / 真隐形）。识别实验优先用 `cn_play_abs`。色彩语义见第三节。
- 优先处理 `design_spec.md` 第八节的**待微调清单**（标题孤字、移动端、可读性、是否更野）。

## 数据可信度

`data/` 里的数字都来自真实采集 + 人工核查（378 条头部已逐条人工裁决），非占位。卡片文案是真实视频 desc 摘录。可放心展示。
**2026-06-27 终态收口**：① 福船的 `junk`/`watertight` 是查询错误词已剔除，福船真相=缺席型(~5万)不是被淹没(1.84亿)；② 全部数字已结合 378 核查锚定，并按"绝对优先、份额作透镜"的 2D 口径重算（collapse 53.68亿→34.33亿）。名字节讲"锚点决定能见度"（celadon 52%→龙泉 73%），不讲"用拼音救场"。
