# 交接日志 — 2026-06-16（人工核查表已分发）

> 项目：UNESCO 中国非遗在 TikTok 美区传播研究 / `tiktok-heritage-crawler`  
> 本轮主线：**把历史相关性口径工程化 → 重算 relevance-aware 触达 → 采 hashtag 存量 → 合成「存量 × 触达」草表 → 生成并分发人工核查优先视频表**  
> 当前状态：等待人工核查表回流；下一步从填写后的 CSV 进入 Phase 5，不改原始 baseline。

---

## 一、本轮完成了什么（按阶段）

### Phase 2.5 — 相关性口径回灌 baseline

1. 复核历史材料：`docs/review/` 与 `data/review/` 下已有人工核查结论与「相关性口径 v1」，没有重新发明口径。
2. 新增 `scripts/derive_video_relevance.py`，把口径 v1 工程化到当前 baseline 视频：
   - 输入：`data/sched_run_20260612_030425/videos.ndjson`
   - 输出：`data/derived/video_relevance_labels.ndjson`
   - 同步输出项目级相关性摘要：`data/derived/project_relevance_summary.csv/json`
3. 标签结果（当前派生版本）：
   - `likely_relevant`: 9,241
   - `needs_review`: 3,488
   - `low_relevance`: 17,864
4. 原始 baseline 未修改；low relevance 不删除，只派生标签。

### Phase 2.6 — relevance-aware 触达维

1. 新增 `scripts/derive_relevance_aware_reach.py`。
2. 输出 `data/derived/project_reach_relevance_aware.csv/json`。
3. 对每个项目并存四套触达口径：
   - `raw`: 原始全部视频触达；保留作诊断。
   - `likely`: 只看 likely relevant；作为当前触达主口径。
   - `inclusive`: likely + needs_review；给人工回流后的移动空间。
   - `low`: low relevance；作为噪声/语义稀释风险，不进入正向触达结论。
4. 关键原则：正式分层不直接拿 raw playCount 下结论，先看 relevance-aware 触达。

### Phase 3 — hashtag 存量维

1. 新增 `scripts/collect_hashtag_scale.py`，采集 44 个项目的 `hashtag_terms`，读取 TikTok `tag.info()` 里的 `statsV2.videoCount`。
2. 输出：
   - `data/derived/hashtag_scale_terms.ndjson`（term 级，155 行）
   - `data/derived/project_hashtag_scale_summary.csv/json`（项目级，44 行）
   - `docs/标签规模分层_tagscale.md`
3. 当前 hashtag 存量状态：
   - term 总数：155
   - `clean`: 118
   - `noisy`: 25
   - `unavailable`: 12
   - 44/44 项目至少有一个 clean hashtag
4. 项目级存量 tier：
   - `head`: 5
   - `mid`: 5
   - `long_tail`: 8
   - `near_invisible`: 26
5. 存量口径：优先取 clean tag 的最大 `statsV2.videoCount`；noisy tag 只作风险提示，不让撞词悄悄抬高项目。

### Phase 4 — 存量 × 触达二维草表

1. 新增 `scripts/derive_stock_reach_matrix.py`。
2. 输出：
   - `data/derived/project_stock_reach_matrix.csv/json`
   - `docs/存量触达二维分层草表.md`
3. 当前 44 项目初版象限分布：
   - `stock_high__reach_high`: 7
   - `stock_high__reach_low`: 3
   - `stock_low__reach_high`: 10
   - `stock_low__reach_low`: 24
4. 当前 review priority 分布：
   - `P0_noise_or_tier_drop`: 21
   - `P0_stock_high_reach_low`: 3
   - `P1_low_stock_high_reach`: 9
   - `P2_noise_risk`: 6
   - `P3_normal`: 5
5. 阈值是草表口径，不是最终结论：
   - 存量高：`head + mid`
   - 触达高：`top_20pct + high_20_40pct`
   - 中间档保守落 lower side，等待人工核查后再定稿。

### Phase 4.5 — 人工核查优先视频表

1. 新增 `scripts/derive_manual_check_priority_videos.py`。
2. 输出：
   - `data/derived/manual_check_priority_videos.csv`
   - `data/derived/manual_check_priority_videos.json`
   - `docs/人工核查优先视频清单.md`
3. 人工表规模：
   - 视频行数：321
   - 覆盖项目：33 个（全部 P0 + P1 项目）
   - 覆盖项目优先级：`P0_noise_or_tier_drop` 21 个、`P0_stock_high_reach_low` 3 个、`P1_low_stock_high_reach` 9 个
4. 抽样 bucket：
   - `likely_evidence`: 108
   - `needs_review_boundary`: 66
   - `low_noise_probe`: 90
   - `raw_top_probe`: 57
5. CSV 已预留人工填写字段：
   - `manual_relevance`
   - `manual_noise_type`
   - `manual_language_reach`
   - `manual_audience_proxy`
   - `manual_notes`
6. 表已分发给人工核查；当前不要继续推断最终分层，等回表。

---

## 二、当前仓库状态

- 工作目录：`/root/workspace/tiktok-heritage-crawler`
- 分支：`main`
- 当前 HEAD：`718bc0f`（写本日志前）
- 远端：`origin/main` 已含本轮四个分析提交
- 原始 baseline 路径：`data/sched_run_20260612_030425/`
- 原始 baseline 未修改。
- 本轮所有分析均为 additive derived artifacts，主要落在：
  - `scripts/`
  - `data/derived/`
  - `docs/`

### 本轮已 push 的提交

1. `de9da7a` — `analysis: apply relevance guideline to baseline reach`
2. `436c458` — `analysis: collect hashtag scale stats`
3. `dd558b7` — `analysis: derive stock reach matrix`
4. `718bc0f` — `analysis: generate manual review priority videos`

本日志提交后会追加一个 handoff/log commit。

---

## 三、接手时先读这些文件

### 方法与决策

- `docs/研究纲领_分层与画像.md`  
  活文件。先读，尤其是「存量 ≠ 触达」「语言三层」「已定决策 vs 假设」相关段落。
- `docs/交接日志_20260612.md`  
  上一阶段交接，记录 search 翻页上限、boundary 第三态、存量/触达概念纠偏。
- `docs/相关性口径v1_baseline应用报告.md`
- `docs/触达维_相关性过滤后分析.md`
- `docs/标签规模分层_tagscale.md`
- `docs/存量触达二维分层草表.md`
- `docs/人工核查优先视频清单.md`

### 数据入口

- 人工核查待回流表：`data/derived/manual_check_priority_videos.csv`
- 人工核查 JSON 对照：`data/derived/manual_check_priority_videos.json`
- 当前草表：`data/derived/project_stock_reach_matrix.csv/json`
- 相关性标签：`data/derived/video_relevance_labels.ndjson`
- relevance-aware 触达：`data/derived/project_reach_relevance_aware.csv/json`
- hashtag 存量：`data/derived/project_hashtag_scale_summary.csv/json`

---

## 四、等待回流的人工字段与建议取值

人工表里真正需要回填的是以下 5 列。后续脚本应容忍空值，只对已填写行生效。

### `manual_relevance`

建议枚举：
- `relevant`：视频本身明确属于该非遗项目/高度相关。
- `boundary`：可能相关，但信息不足或只沾边。
- `irrelevant`：明显误召回/撞词/泛化内容。
- `unknown`：打不开、无法判断、内容缺失。

### `manual_noise_type`

建议枚举，可为空：
- `keyword_collision`：关键词撞词。
- `generic_culture`：泛文化/泛中国风，不指向该项目。
- `commercial_or_product`：商品/商业素材为主。
- `fan_edit_or_meme`：二创/梗/剪辑导致误召回。
- `wrong_project`：相关但属于另一个项目。
- `platform_artifact`：平台/元数据异常。
- `other`：其他，写入 notes。

### `manual_language_reach`

建议枚举：
- `cjk_primary`
- `english_primary`
- `mixed`
- `non_cjk_non_english`
- `no_speech_or_unclear`

这是「被看到的视频内容语言」的代理，不等于受众语言。

### `manual_audience_proxy`

建议枚举：
- `comments_cjk_primary`
- `comments_english_primary`
- `comments_mixed`
- `few_or_no_comments`
- `not_checked`

这是评论区语言的小样本代理，不代表真实受众构成。

### `manual_notes`

自由文本；记录判断依据、打不开链接、特别典型的噪声/真相关样例。

---

## 五、Phase 5 回流方案（表回来后做）

### 目标

新增一个 additive 回流脚本，合并人工核查结果，输出「正式分层定稿」草案。不要覆盖原始 baseline，不要覆盖原始自动标签；生成人工校正后的 derived artifacts。

### 建议新增脚本

`scripts/apply_manual_review_to_matrix.py`

### 输入

- 填写后的 `data/derived/manual_check_priority_videos.csv`
- `data/derived/project_stock_reach_matrix.csv`
- `data/derived/project_reach_relevance_aware.csv`
- `data/derived/project_hashtag_scale_summary.csv`
- 可选：`data/derived/video_relevance_labels.ndjson` 用于视频级对照

### 输出建议

- `data/derived/manual_review_project_summary.csv/json`
  - 每项目人工核查行数
  - relevant/boundary/irrelevant/unknown 计数
  - likely bucket 的人工通过率
  - raw/low bucket 的噪声确认率
  - top/raw 误召回案例数
  - 语言与评论区代理摘要
- `data/derived/project_stock_reach_matrix_manual_adjusted.csv/json`
  - 在原草表基础上追加人工校正字段
  - 不删除原字段
- `docs/正式分层定稿.md`
  - 分层最终版 + 人工核查如何改变判断 + 局限声明

### 回流判定建议

1. **P0_noise_or_tier_drop**  
   如果 raw top / low_noise_probe 大量被人工判 `irrelevant`，保持 raw 噪声警告；正式触达继续以 likely 或人工确认的相关视频为主。
2. **P0_stock_high_reach_low**  
   重点验证是不是「内容存量高但真的没人看」。如果 likely_evidence 也弱、raw top 又多为噪声，可定为「自产自销/虚假繁荣」候选。
3. **P1_low_stock_high_reach**  
   重点验证是不是「小而精破圈」。如果高播放样本人工确认 relevant，则保留为重要发现；若多为误召回，降级。
4. **needs_review_boundary**  
   只在人工判 relevant 且播放贡献足够大时，才允许 inclusive 影响最终 reach tier。
5. **语言/受众代理**  
   只作为 qualitative corroboration；不要把评论区语言写成真实受众比例。

### 推荐新增字段

在人工校正版 matrix 里追加：

- `manual_rows_reviewed`
- `manual_relevant_rows`
- `manual_boundary_rows`
- `manual_irrelevant_rows`
- `manual_unknown_rows`
- `manual_relevant_play_sum`
- `manual_irrelevant_play_sum`
- `manual_likely_pass_rate`
- `manual_raw_top_noise_confirmed`
- `manual_low_bucket_noise_confirmed`
- `manual_language_reach_summary`
- `manual_audience_proxy_summary`
- `manual_adjusted_reach_band`
- `manual_adjusted_quadrant`
- `manual_final_tier`
- `manual_review_note`

### 最小验证

回流脚本跑完至少检查：

```bash
/root/workspace/tiktok-api-venv/bin/python3 -m py_compile scripts/apply_manual_review_to_matrix.py
python3 scripts/apply_manual_review_to_matrix.py
python3 - <<'PY'
import csv
for p in [
  'data/derived/manual_review_project_summary.csv',
  'data/derived/project_stock_reach_matrix_manual_adjusted.csv',
]:
    rows=list(csv.DictReader(open(p, encoding='utf-8-sig')))
    print(p, len(rows), rows[0].keys())
PY
```

确认项目数仍为 44；人工 summary 至少覆盖 33 个已分发表项目；空人工字段不会导致脚本崩溃。

---

## 六、不要踩的坑

1. **不要改 baseline 原始文件**：`data/sched_run_20260612_030425/term_results.ndjson`、`videos.ndjson` 只读。
2. **不要删除 low relevance**：它是噪声风险信号，后续报告要用。
3. **不要把 hashtag videoCount 写成传播成功**：它是存量，不是触达。
4. **不要用 raw playCount 直接定最终层级**：raw 高常常是撞词或泛内容，要看 likely/人工确认。
5. **不要把评论区语言说成真实受众比例**：只能叫有偏代理、小样本佐证。
6. **不要 silent fallback / silent downgrade**：如果人工表字段缺失、枚举混乱、回流无法稳健判断，要在报告里显式列出。
7. **提交习惯**：每完成一个有意义节点就 commit + push；`data/` 被 gitignore 时，对需要纳入版本的 derived 文件用 `git add -f`。

---

## 七、当前下一步一句话

等人工核查后的 `manual_check_priority_videos.csv` 回来后，写并运行 `scripts/apply_manual_review_to_matrix.py`，生成 `manual_review_project_summary`、`project_stock_reach_matrix_manual_adjusted` 和 `docs/正式分层定稿.md`，验证 44 项目完整后 commit + push。
